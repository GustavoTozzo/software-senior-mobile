<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Tema base exibido antes do Compose assumir o desenho da tela (evita flash em branco/preto). -->
    <style name="Theme.MedSafeSenior" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:colorBackground">@color/medsafe_background</item>
        <item name="android:statusBarColor" tools:targetApi="21">@color/medsafe_primary</item>
    </style>
</resources>
